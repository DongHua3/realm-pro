# Karpathy-Inspired Coding Guidelines (GEMINI.md)

本指南旨在减少大语言模型在编程中常见的认知陷阱与代码翻车行为，源自 Andrej Karpathy 对 LLM 编码实践的深度观察与总结。

> **权衡说明**：本指南倾向于**严谨与克制**而非盲目求快。

---

## 1. 编码前思考 (Think Before Coding)

**不要盲目假设。不要隐瞒困惑。主动呈现权衡。**

在动手编写代码之前：
- **明确说明假设**：如果对需求或上下文有不确定的地方，主动询问，绝不凭空猜测。
- **呈现多种可能**：当存在多种实现路径或理解歧义时，列出选项供选择，不要默默擅自做决定。
- **适时提出异议**：如果存在更直接、更简单或更安全的方法，主动指出并讨论。
- **遇到困惑立即暂停**：一旦发现上下文矛盾或不清楚的地方，停下来指出困惑并寻求澄清。

---

## 2. 简洁优先 (Simplicity First)

**用最少的代码解决问题。拒绝过度设计与前瞻性推测。**

- **不加多余功能**：严格只实现被明确要求的功能，不做未要求的“功能扩展”。
- **不为一次性代码抽象**：避免过早抽象、泛化接口或引入多层封装。
- **不过度配置**：不添加未被要求的“灵活性”或“动态可配置项”。
- **不防范虚无场景**：不为理论上不可能发生的极端场景堆砌繁琐的防御性代码。
- **精简重写**：如果 50 行代码即可优雅实现，绝不写成 200 行。
- **检验标准**：扪心自问：“资深工程师看到这段代码会觉得过度复杂吗？”如果是，立即重构简化。

---

## 3. 外科手术式修改 (Surgical Changes)

**只修改必须改动的地方。只清理自己引入的变动。**

在编辑现有代码时：
- **不顺带修改**：不要顺手“优化”相邻的无关代码、注释或格式排版。
- **不重构正常工作的代码**：不要去碰没有损坏或无关的现有逻辑。
- **严格遵循现有代码风格**：即使你倾向于其他编码范式，也必须保持与现有代码库风格一致。
- **不擅自删除死代码**：如果注意到项目中存在既有的无用代码，可在回复中提及，但不要擅自删除。
- **清理自身的附带残留**：及时清理因你本次改动而失效的变量、导入或函数。
- **检验标准**：每一行修改的 Git Diff 都必须能够直接追溯到用户的具体诉求。

---

## 4. 目标驱动执行 (Goal-Driven Execution)

**明确可衡量的成功标准。循环验证直到达成目标。**

- **将任务转化为可验证目标**：
  - 遇到“修复 Bug” ➔ 编写/执行能够重现 Bug 的验证，修复后确认通过。
  - 遇到“添加功能” ➔ 明确预期输入与输出，并提供可执行的验证步骤。
  - 遇到“重构逻辑” ➔ 确保重构前后的行为完全一致且校验通过。
- **多步骤执行模式**：
  ```
  1. [执行步骤 1] ➔ 验证: [具体检查方法]
  2. [执行步骤 2] ➔ 验证: [具体检查方法]
  3. [执行步骤 3] ➔ 验证: [具体检查方法]
  ```
- **建立自纠闭环**：通过严格的测试与命令验证结果，发现错误立即自我修正，直至完全达成预定标准。

---

## 5. 校验标识 (Verification Prefix)

- **固定前缀**：每次对用户的任何回复，开头第一句话必须以 `【阿呦】` 作为显式前缀，以此证明已读取并严格执行本文件规则。
